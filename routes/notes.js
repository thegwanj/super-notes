const notes = require('express').Router();
const app = require('.');
const db = require('../db/db.json');

app.get('/api/notes', (req, res) => {

});

app.post('/api/nots', (req, res) => {

});

module.exports = notes;